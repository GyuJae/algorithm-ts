describe('단일 연결 리스트', () => {
  it('push()', () => {
    it.todo('value 값을 받아야 합니다.');

    it.todo('value 값의 새로운 NodeItem을 만들어여 합니다.');

    it.todo(
      '빈 List일 시 head 값과 tail값을 새로운 NodeItem을 가르켜야 합니다.'
    );

    it.todo(
      'list에 새로운 값이 들어왔을 시에 tail값에 새로운 NodeItem을 가르켜야 한다.'
    );

    it.todo('length값이 하나 올라가야한다.');
  });
});
